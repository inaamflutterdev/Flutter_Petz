import '../../../core/app_export.dart';/// This class is used in the [line_item_widget] screen.
class LineItemModel {LineItemModel({this.group, this.id, }) { group = group  ?? "Dog";id = id  ?? ""; }

String? group;

String? id;

 }
