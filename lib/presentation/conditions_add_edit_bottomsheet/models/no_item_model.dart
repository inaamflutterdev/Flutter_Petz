import '../../../core/app_export.dart';/// This class is used in the [no_item_widget] screen.
class NoItemModel {NoItemModel({this.no, this.isSelected, }) { no = no  ?? "No";isSelected = isSelected  ?? false; }

String? no;

bool? isSelected;

 }
