import '../../../core/app_export.dart';/// This class is used in the [dynamictext_item_widget] screen.
class DynamictextItemModel {DynamictextItemModel({this.dynamicText, this.id, }) { dynamicText = dynamicText  ?? "Albon";id = id  ?? ""; }

String? dynamicText;

String? id;

 }
